apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocdu_dagster.fullname" . }}
  labels:
    {{- include "argocdu_dagster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocdu_dagster.selectorLabels" . | nindent 4 }}
